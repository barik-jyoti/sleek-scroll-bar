import { CustomScrollDirective } from './custom-scroll.directive';

describe('CustomScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomScrollDirective();
    expect(directive).toBeTruthy();
  });
});
